# Plantilla de credenciales. Copiar a `.env` y rellenar con las propias.
# El `.env` real está en .gitignore y no debe subirse nunca.
DB_USER=usuario_mysql
DB_PASS=contrasena
DB_HOST=localhost
DB_PORT=3306
DB_NAME=nombre_de_la_base
